# Encargo a Codex — E1 (teléfono de 10 dígitos y fuera WhatsApp del cliente)

Lee `AGENTS.md` en la raíz ANTES de tocar nada: reglas duras, no negociables.
Colores sólo de los tokens de `apps/admin/src/styles/tokens.css`. Escala
tipográfica del proyecto, nunca `text-sm` ni tamaños arbitrarios. Primitivas de
`components/ui/page.tsx`. Nunca definas un componente dentro de otro componente.

## ⚠ La trampa cara de este paso: hay TRES campos `whatsapp` distintos

- **`Customer.whatsapp`** → es el que se quita. Sólo éste.
- **`Settings.whatsapp`** → obligatorio, y es el que se imprime en el encabezado
  de **todos** los PDFs (`pdf.service.ts` → `templates/quotation.hbs`).
  **No lo toques.**
- **`QuoteRequest.whatsapp`** → es del cotizador público. **No lo toques.**

Confundirlos rompe los PDFs. Antes de borrar cualquier cosa, comprueba a cuál de
los tres pertenece.

## La tarea — paso E1 del plan

El plan está en `Plans/ajustes-insumos-productos-cotizacion/plan.md`, sección E1.

Archivo principal: `apps/admin/src/features/customers/CustomersPage.tsx`.
Secundarios: `apps/api/src/modules/sales/customers/dto/create-customer.dto.ts` y
`apps/admin/src/lib/types.ts`.

- El campo "Teléfono" acepta **exactamente 10 dígitos**: en el formulario con
  `inputMode="numeric"`, `maxLength` y `pattern`; en la API con `@Matches` en el
  DTO. Hoy **no hay ninguna validación en ninguna capa** y se acepta `"asdf"`.
  El teléfono sigue siendo opcional si hoy lo es: la regla de 10 dígitos aplica
  cuando viene un valor, no obliga a capturarlo.
- El campo **WhatsApp del cliente** desaparece: del formulario, de la columna de
  la tabla, del payload y del DTO de la API.
- **NO debe cambiar:** la columna `Customer.whatsapp` se queda en la base sin
  migración; sólo se deja de usar. No toques `prisma/schema.prisma`.

## Reglas

- Sin dependencias nuevas. Sin abstracciones que no se pidieron. El diff más
  corto.
- **NO toques `QuotationFormPage.tsx` ni crees nada en
  `features/customers/quick-create-dialog.tsx`**: hay otro agente trabajando ahí
  en este momento.
- NO hagas commit ni push.

## Sobre ejecutar comandos

Tu shell no puede ejecutar `pnpm`, `npx` ni `node`. No pierdas tiempo
intentándolo: dilo y ya. Yo corro build, lint y la app.

## En tu resumen

Qué regex usaste, qué pasa con un cliente ya guardado cuyo teléfono no cumple
(¿se puede seguir editando?), y confirma explícitamente que no tocaste
`Settings.whatsapp` ni `QuoteRequest.whatsapp`.

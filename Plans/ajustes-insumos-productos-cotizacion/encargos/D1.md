# Encargo a Codex — D1 (el aroma de la cotización pasa a ser un dropdown)

Lee `AGENTS.md` en la raíz ANTES de tocar nada: reglas duras, no negociables.
Colores sólo de los tokens de `apps/admin/src/styles/tokens.css`. Escala
tipográfica del proyecto (`text-body-sm`, `text-caption`...), nunca `text-sm` ni
tamaños arbitrarios. `aria-label` en todo botón de sólo icono. Nunca definas un
componente dentro de otro componente.

## Estado actual

Los tipos de insumo ya son una tabla: un insumo trae `type` como objeto, con
`type.slug`. El aroma es el slug `FRAGRANCE`. Ya existe el hook
`apps/admin/src/hooks/use-supply-options.ts`, que lista insumos.

## La tarea — paso D1 del plan

El plan está en `Plans/ajustes-insumos-productos-cotizacion/plan.md`, sección D1.
Archivo principal:
`apps/admin/src/features/quotations/components/quotation-line-item-editor.tsx`
(alrededor de las líneas 110-114).

- "Nombre del aroma" deja de ser un campo de texto libre y se vuelve un
  `Select` con `searchable`, con los insumos de tipo aroma
  (`supply.type.slug === 'FRAGRANCE'`) que además tengan `stockQty > 0`.
- **Se guarda el nombre del insumo en el campo `fragranceName`, que ya existe.**
  No se toca Prisma, ni el DTO, ni el motor de costeo: sigue viajando un texto.
- Si un renglón ya guardado trae un aroma que ya no está en la lista (se acabó
  la existencia, o se dio de baja el insumo), **ese valor se conserva visible**
  en el select en vez de borrarse solo. Un renglón viejo no puede perder su
  aroma porque hoy no haya stock.
- Al apagar el switch "Con aroma", el campo se limpia. Hoy queda un aroma
  huérfano guardado en un renglón que ya no lleva aroma.

## NO debe cambiar

- Ningún otro campo del renglón, ni el `switch` de "Con aroma", ni el recargo
  por aroma.
- El hook `use-supply-options.ts`: si necesitas filtrar por aroma, hazlo en el
  editor con los `supplies` que el hook ya devuelve, o pásale una opción — pero
  no cambies su comportamiento actual para los demás llamadores.

## Reglas

- Sin dependencias nuevas. Sin abstracciones que no se pidieron. El diff más
  corto.
- NO toques `apps/api/`, ni `prisma/`, ni `packages/`.
- NO toques `QuotationFormPage.tsx`: hay otro trabajo pendiente ahí.
- NO hagas commit ni push.

## Sobre ejecutar comandos

Tu shell no puede ejecutar `pnpm`, `npx` ni `node`. No pierdas tiempo
intentándolo: dilo y ya. Yo corro build, lint y la app.

## En tu resumen

Cómo resolviste el caso del aroma guardado que ya no está en la lista, y cómo
limpias el campo al apagar el switch.
